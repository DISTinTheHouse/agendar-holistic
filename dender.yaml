services:
  - type: web
    name: holistica-citas
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn holistica_citas.wsgi:application
    envVars:
      - key: DEBUG
        value: false
      - key: SECRET_KEY
        value: reemplaza-esto-por-una-segura
